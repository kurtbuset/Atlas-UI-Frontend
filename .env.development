# Development Environment Configuration
# Use fake backend for local development
VITE_USE_FAKE_BACKEND=true

# API Base URL (not used when fake backend is enabled)
VITE_API_BASE_URL=http://localhost:3000
